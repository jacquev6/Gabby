{
  "name": "gabby",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build  # @todo Document use or remove",
    "preview": "vite preview  # @todo Document use or remove",
    "test:e2e": "start-server-and-test 'vite --port 4173 --strictPort' http://localhost:4173/ 'cypress run --e2e --config baseUrl=http://localhost:4173/'",
    "test:unit": "cypress run --component"
  },
  "dependencies": {
    "@floating-ui/vue": "^1.0.4",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.2",
    "pdfjs-dist": "^2.4.456",
    "vue": "^3.3.11",
    "vue-i18n": "^9.9.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "cypress": "^13.6.3",
    "start-server-and-test": "^2.0.3",
    "vite": "^5.0.10"
  }
}
